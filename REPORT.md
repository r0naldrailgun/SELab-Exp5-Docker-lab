# گزارش کار آزمایش پنجم: Docker

## هدف آزمایش

هدف، اجرای برنامه های داده شده `server.py` و `client.py` در دو کانتینر جداگانه، بدون تغییر کد پایتون، و برقرار کردن ارتباط آن ها با Docker Compose بود.

## فایل های پروژه

- `server.py`: یک HTTP server پایتونی که در پورت داخلی `80` پاسخ متنی می دهد.
- `client.py`: آدرس سرور را از متغیر محیطی `SERVER_HOST` می خواند و پنج بار درخواست ارسال می کند.
- `Dockerfile.server`: با image پایه `python:3.10-alpine`، فایل سرور را کپی و اجرا می کند.
- `Dockerfile.client`: با image پایه `python:3.10-alpine`، فایل کلاینت را کپی و اجرا می کند.
- `docker-compose.yml`: سرویس های `my-server` و `my-client` را تعریف می کند؛ پورت `8000` میزبان را به پورت `80` سرور متصل کرده و `SERVER_HOST=my-server` را برای کلاینت قرار می دهد.

## روند انجام کار

1. فایل های پایتون داده شده، دو Dockerfile جداگانه و `docker-compose.yml` ایجاد شدند.
2. ابتدا build به علت دریافت نکردن image پایه از Docker Hub با خطای `403 Forbidden` متوقف شد.
3. برای رفع محدودیت دسترسی، registry mirrorها در Docker Desktop از مسیر `Settings > Docker Engine` تنظیم شدند.
4. پس از اعمال تنظیمات، دستور زیر با موفقیت اجرا شد:

   ```powershell
   docker compose up --build
   ```

5. Docker هر دو image را ساخت، شبکه پیش فرض Compose را ایجاد کرد و هر دو کانتینر را اجرا کرد.

## نتیجه اجرا

کلاینت با نام سرویس `my-server` به سرور متصل شد و پاسخ HTTP دریافت کرد. اولین تلاش کلاینت با خطای `Connection refused` همراه بود؛ زیرا کلاینت پیش از آماده شدن server آغاز شد. چهار تلاش بعدی با موفقیت انجام شد و پیام زیر دریافت شد:

```text
Hello! The answer was sent from the Docker server container.
```

سرور نیز برای درخواست های موفق، پاسخ HTTP با وضعیت `200` ثبت کرد. خروج طبیعی کلاینت با کد `0` نشان می دهد که اجرای برنامه پایان موفق داشته است.

## دستورات مورد نیاز برای ارائه و گزارش

```powershell
docker compose up --build
curl http://localhost:8000
docker compose ps -a
docker logs <client_container_name>
docker exec -it <server_container_name> ls
```

برای داشتن پنج پاسخ موفق در لاگ، پس از آماده شدن سرور می توان کلاینت متوقف شده را با دستور زیر دوباره آغاز کرد:

```powershell
docker compose start my-client
```

## پاسخ پرسش های نظری

1. `docker-compose.yml` سرویس های وابسته، build، پورت ها و متغیرهای محیطی یک برنامه چندکانتینری را در یک فایل تعریف می کند. برای اجرای چند سرویس مرتبط از Compose استفاده می شود؛ `docker run` بیشتر برای اجرای دستی یک کانتینر است.
2. Kubernetes برای استقرار، مقیاس دهی، شبکه سازی و مدیریت خودکار کانتینرها در چند ماشین به کار می رود. Docker image و container فراهم می کند و Kubernetes می تواند این کانتینرها را در مقیاس بزرگ مدیریت کند.
3. Image یک الگوی فقط خواندنی شامل برنامه و وابستگی های آن است. Container نمونه در حال اجرای یک image است. Volume فضای ذخیره سازی پایداری است که داده را مستقل از عمر container نگه می دارد.

## استفاده از هوش مصنوعی

برای تحلیل صورت مسئله، ساخت راهنمای مرحله به مرحله، ایجاد فایل های موردنیاز و رفع خطای دریافت image از Docker Hub از Codex مبتنی بر GPT-5 استفاده شد. درخواست های اصلی شامل «plan and analyze it»، «give me a step by step guide»، «create them»، و راهنمای رفع خطای `403 Forbidden` بودند. پاسخ ها برای ساخت فایل های Docker، اجرای دستورات موردنیاز و پیکربندی registry mirrorها استفاده شدند.

مزیت استفاده از این ابزار، دریافت راهنمای مرحله به مرحله و تشخیص سریع علت خطای شبکه بود. محدودیت آن این است که نتیجه باید با اجرای واقعی دستورات Docker و بررسی خروجی ها توسط دانشجو تأیید شود.
